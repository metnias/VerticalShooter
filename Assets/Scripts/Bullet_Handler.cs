using UnityEngine;

public class Bullet_Handler : MonoBehaviour
{
    public int power = 1;
    public bool isBoom = false;
    // if this wasn't a MonoBehaviour, this could've been just a struct :/
}
